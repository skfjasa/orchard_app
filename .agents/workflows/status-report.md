# Status Report Workflow

Use when the user says `status report`.

1. Read `C:\Users\skfja\Projects\AGENTS.md`.
2. Read `AGENTS.md`.
3. Read `.agents/current.md`.
4. Read `.agents/next.md`.
5. Inspect git branch, status, and latest commit.
6. Do not edit files.
7. Do not read long docs or archived handoffs unless `.agents/current.md` says they are needed.

Output branch, dirty files, latest commit, current objective, validation state, likely next files, and recommended next step. Then wait for the user.
