# Session Handoff - 2026-07-12

## Goal

Implement corrected backlog item 6 transactional profile-photo replacement and, separately, harden the reported email-confirmation redirect regression.

## Photo Replacement Completed

- Added forward-only migration `202607110002_transactional_profile_photo_replacement.sql`; item 4 migration `202607110001` was not edited.
- Added authenticated RPC `replace_profile_photos`, scoped through `auth.uid()`, with empty `search_path`, authenticated-only grant, owner/member/path validation, duplicate rejection, locking, atomic targeted replacement, and authoritative committed/displaced results.
- Replaced client row-by-row mutations with upload-all -> one RPC -> post-commit displaced-object cleanup.
- Awaited all compensation and cleanup operations. Upload/RPC failures clean new attempt objects and preserve old metadata/objects.
- Post-commit deletion failures return an explicit warning while retaining valid committed metadata and allowing item 5 finalization.
- Preserved unchanged paths/moderation state, item 4 ownership, private storage, signed URL presentation, and mock/demo behavior.
- Implementation commit: `e9d3f1b` - Make profile photo replacement transactional.

## Auth Redirect Fix Completed

- Added a pure resolver that normalizes web origins and full callbacks to `/onboarding/sign-in`, avoids duplicate paths/slashes, uses dynamic web origin as fallback, and preserves native Expo/deep-link behavior.
- Signup confirmation and password reset now share the resolver.
- Updated safe environment/setup guidance; the temporary tunnel is not a production default.
- Updated ignored local `expo/.env` to `https://maturely-usher-electable.ngrok-free.dev/onboarding/sign-in`; it was not staged.
- Hosted URL Configuration, Site URL, and email templates were unavailable and were not inspected or changed. New confirmation/recovery email UAT remains required.
- Implementation commit: `88a1f2a` - Fix hosted auth callback redirect.

## Validation

- `cd expo; bun test`: 67 passing tests, 125 assertions.
- `cd expo; bun run typecheck`: passed.
- `cd expo; bun run lint`: passed.
- Full local Supabase reset: passed through `202607110002` and seed.
- Full pgTAP suite: 1 file, 103 passing cases.
- Database lint at warning level: passed with no findings.
- Targeted RPC/grant/result/ownership inspection: passed.
- `git diff --check`: passed apart from normal LF-to-CRLF working-copy notices.

## Remaining

- Item 2 remains open.
- Item 3 remains awaiting UAT.
- Item 4 remains awaiting hosted preflight, apply, and UAT.
- Item 5 remains awaiting human UAT.
- Item 6 is `Implemented / Automated Checks Passed / Awaiting Hosted UAT`.
- Auth redirect is `Repository Fix Complete / Hosted Configuration or Email UAT Pending`.
- Neither July migration was applied to hosted Supabase.
- Corrected backlog item 7 was not started and requires explicit selection.
