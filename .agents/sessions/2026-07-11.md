# Session Handoff - 2026-07-11

## Goal

Implement corrected audit backlog item 4: enforce profile-photo storage-path ownership.

## Completed

- Added forward-only migration `202607110001_profile_photo_storage_path_ownership.sql`.
- Added immutable `<profile_id>/<purpose>/<filename>` validation, a fail-fast existing-row preflight, and a table CHECK that also protects RLS-bypassing writes.
- Hardened `profile_photos` INSERT and UPDATE policies with authenticated profile and path ownership checks.
- Preserved the private bucket, signed URL flow, discovery/active-match reads, and already-correct owner-prefixed storage object write policies.
- Expanded the database suite from 45 to 67 cases covering valid writes, attacker INSERT/UPDATE attempts, privileged bypass, visibility, active-match reads, block revocation, and preflight detection.
- Documented the hosted preflight query and deliberately deferred same-owner path uniqueness as a separate lifecycle concern.
- Published implementation checkpoint `ddac497` - Enforce profile photo path ownership.
- Did not apply the migration to hosted Supabase or begin corrected backlog item 5.

## Validation

- Full local Supabase reset: passed through migration `202607110001` and seed.
- Full pgTAP suite: passed, 1 file and 67 tests.
- Database lint: passed with no schema errors.
- Targeted local SQL inspection: constraint and policies present; storage SELECT preserved; invalid local rows = 0.

## Remaining

- Run the read-only hosted metadata preflight before applying the migration.
- If preflight is clean, apply only with explicit user instruction and complete owner/attacker/visibility/block/unmatch hosted UAT.
- Status: `Implemented / Automated Checks Passed / Awaiting Hosted UAT`.

## Corrected Audit Backlog Item 5

### Goal

Make Supabase onboarding completion two-phase without beginning item 2, item 6, or broader bootstrap redesign work.

### Completed

- Added distinct incomplete preparation, incomplete retry, finalization, and ordinary edit mappings.
- Added a pure prepare -> members -> settings -> photos -> finalize orchestrator and wired it to the authenticated current profile.
- Unique-conflict retries inspect the server row; incomplete rows remain incomplete/invisible, while completed rows return authoritatively without being reset or overwritten.
- Member/settings/photo failures stop before finalization. Finalization errors and zero-row updates return `profile_finalization_failed`.
- Added missing/incomplete/completed profile read outcomes, pending-draft resumption, incomplete-profile protected-route gating, and safe reconstruction through the existing onboarding entry.
- Preserved mock/demo onboarding and email-confirmation behavior. Supabase attempts refresh credential-stripped pending onboarding before persistence and clear it only after success.
- Added focused Bun tests and pgTAP coverage. No migration or RPC was added; existing discovery/RLS predicates already require completion and visibility and allow owner resumption reads.
- Published implementation checkpoint `96c25bc` - Make onboarding completion two-phase.

### Validation

- `cd expo; bun test`: passed, 35 tests and 72 assertions.
- `cd expo; bun run typecheck`: passed.
- `cd expo; bun run lint`: passed.
- Full local Supabase reset: passed through `202607110001` and seed.
- Full pgTAP suite: passed, 1 file and 75 tests.
- Database lint at warning level: passed with no findings.
- `git diff --check`: passed with only existing LF-to-CRLF warnings.

### Remaining

- Item 5 status: `Implemented / Automated Checks Passed / Awaiting UAT`.
- Run the documented immediate-session, confirmation-required, member/settings/photo/finalization failure and retry, incomplete relaunch, completed-profile regression, and mock/demo UAT.
- Item 2 remains open. Item 3 remains awaiting UAT. Item 4 remains awaiting hosted preflight/apply/UAT.
- Migration `202607110001` was not edited or applied to hosted Supabase.
- Item 6 transactional photo replacement, displaced-path cleanup, and orphan cleanup were not started.
