# Session Handoff - 2026-07-07

## Completed

- Added one-command mobile web UAT launcher: `cd expo; bun run uat-web-tunnel`.
- Avoided Expo's stale bundled ngrok v2 tunnel path by using a standalone global ngrok v3 agent.
- Reintroduced focused web browser-back handling in `useCanonicalBack` so Chat and Match Detail browser/device back return to their canonical tab routes.
- Human UAT accepted Slice 1 navigation cleanup:
  - Android Chat passed.
  - Desktop Chat passed.
  - Desktop Match Detail passed.
  - Android Match Detail passed without white browser reload; a brief app-background loader appears during early repeated backs and then warms up.
- Fixed real/dev tester-reported forgot-password gap:
  - `Forgot password?` now sends a Supabase reset email.
  - Auth callback parsing detects recovery links.
  - `/onboarding/sign-in` shows an in-app new-password form during recovery.
  - Password update calls `supabase.auth.updateUser({ password })`.
- Updated milestone/status/handoff docs.

## Validation

- `cd expo; bun run typecheck`: passed.
- `cd expo; bun run lint`: passed.
- `git diff --check`: passed.

## Commits

- `0eb91af` - Add UAT tunnel and auth recovery fixes
- Handoff sync commit is expected after this session note is committed.

## Open UAT

Forgot-password still needs human UAT on hosted Supabase/ngrok:

1. Request a reset email from the sign-in screen.
2. Open the reset link on the same browser/device.
3. Confirm Orchard shows the new-password form.
4. Set a new password.
5. Sign in with the new password.
6. Confirm the old password no longer works.

## Next

After forgot-password UAT, start foundation Slice 2: update/freeze the `ProfileProvider` facade contract and responsibility inventory.
