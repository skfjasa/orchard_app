# 2026-07-05 Session

## Summary

Focused on hosted Supabase UAT issues around device/browser back navigation from Matches and Inbox:

- Rapid back from a profile opened through Matches could show missing real/dev matches, generic "Orchard user" profile data, or snap back onto the same profile detail.
- Rapid back from a conversation opened through Inbox could show missing conversations or generic profile rows.
- Fruit could restore state, suggesting profile display/cache hydration was still too dependent on later discovery refreshes.

## Commits

- `ae3a9aa` - Prevent generic backend profile fallback
- `06934ce` - Harden back navigation profile state
- `9afe644` - Stabilize profile back stack display

All commits were pushed to `origin/main`.

## Final Runtime Changes

- `expo/providers/profile-provider.tsx`
  - Rejects incomplete backend profile objects that only contain generic "Orchard user" identity.
  - Keeps a display-profile snapshot keyed by profile id so Matches/Inbox/detail can use the last complete profile during refresh churn.
  - Preserves remembered match profiles across same-user Supabase token refreshes.
  - Queues a follow-up backend match refresh when a refresh request arrives while another refresh is already in flight.
- `expo/hooks/use-canonical-back.ts`
  - Uses `router.dismissTo(...)` instead of `router.replace(...)` for canonical back routing.
- `expo/app/_layout.tsx`
  - Changes `match/[id]` from root modal presentation to normal stack presentation to avoid modal back-stack conflicts.

## Validation

- `cd expo; bun run typecheck` passed.
- `cd expo; bun run lint` passed.
- `git diff --check` passed.
- Final git state after push: clean and synced with `origin/main` at `9afe644`.

## Needs UAT Next

1. Sign in as `t`, `tt`, or `test2`.
2. From Matches, open a real/dev profile and use device/browser back repeatedly.
3. Confirm Matches still shows real/dev profiles, not only fixtures.
4. Confirm no generic "Orchard user" profile/card appears.
5. Confirm profile detail does not animate back then snap onto itself.
6. From Inbox, open a real/dev conversation and use device/browser back repeatedly.
7. Confirm Inbox still shows real/dev conversations, unread state remains stable, and Fruit/Discover is not required to restore rows.

## Remaining Risk

The latest changes are unconfirmed by user UAT. If symptoms persist, likely next investigation should move from local cache/render fixes to inspecting actual hosted profile/member rows for affected dev accounts and testing whether browser history is serving stale bundle/state during ngrok sessions.
